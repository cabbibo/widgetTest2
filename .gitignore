# dependencies
node_modules/

# build output (root workspace target + per-widget dist)
dist/
target/

# vendored three.js bundle (resolved from the npm package at build time)
**/src/three.js

.DS_Store
